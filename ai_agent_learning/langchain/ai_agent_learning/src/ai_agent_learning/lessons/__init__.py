"""Standalone, runnable lessons.

Each file is one idea and runs on its own:

    python -m ai_agent_learning.lessons.lesson_01_llm_call
    python -m ai_agent_learning.lessons.lesson_02_tool_direct

They are kept as real scripts rather than commented-out code because
commented code rots — it never runs, so it breaks silently when a library
changes and you find out months later.
"""
